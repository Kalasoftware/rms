<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style>
      body { background: #fff; }
      .expenses-card {
        background: #fff0f3;
        border-radius: 1.5rem;
        box-shadow: 0 2px 12px rgba(139,10,26,0.08);
        padding: 2rem 2rem 1.5rem 2rem;
        max-width: 900px;
        margin: 2rem auto;
        border: 1.5px solid #8B0A1A;
      }
      .expenses-title {
        font-weight: 700;
        font-size: 2rem;
        margin-bottom: 1.5rem;
        color: #8B0A1A;
        text-align: center;
      }
      .expenses-summary {
        font-size: 1.2rem;
        color: #8B0A1A;
        margin-bottom: 1.5rem;
        text-align: center;
      }
      .table th, .table td {
        vertical-align: middle;
      }
      .table thead {
        background: #8B0A1A;
        color: #fff;
      }
      .table tbody tr:hover {
        background: #fbe4e7;
      }
    </style>
</head>
<body>
    <div class="expenses-card">
      <div class="expenses-title"><i class="bi bi-receipt"></i> Expenses Report</div>
      <div class="expenses-summary">
        <strong>Today's Total Expenses:</strong> ₹<%= expensesToday %>
      </div>
      <div class="table-responsive">
        <table class="table table-bordered table-hover">
          <thead>
            <tr>
              <th>Date</th>
              <th>Amount</th>
              <th>Description</th>
              <th>Category</th>
            </tr>
          </thead>
          <tbody>
            <% expenses.forEach(exp => { %>
              <tr>
                <td><%= exp.createdAt ? exp.createdAt.toLocaleString() : '-' %></td>
                <td>₹<%= exp.amount.toFixed(2) %></td>
                <td><%= exp.description %></td>
                <td><%= exp.category ? exp.category.name : '-' %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
      <a href="/dashboard" class="btn btn-modern mt-2" style="background:#8B0A1A; color:#fff;"><i class="bi bi-arrow-left"></i> Back to Dashboard</a>
    </div>
</body>
</html>
